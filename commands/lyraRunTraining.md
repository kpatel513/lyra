Run the training command provided for 100 steps and summarize and root cause any failures if any.
Run the command using `source /home/ubuntu/miniforge3/etc/profile.d/conda.sh && conda activate niche_env && python downstream_fine_tune.py --log-level INFO`
Stop the training command after 100 steps